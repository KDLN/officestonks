[phases.setup]
nixPkgs = ["go", "nodejs"]

[phases.install]
cmds = [
    "npm --prefix frontend install",
    "go mod download"
]

[phases.build]
cmds = [
    "npm --prefix frontend run build",
    "go build -o out ./cmd/api"
]

[start]
cmd = "./out"